# ─────────────────────────────────────────────────────────────────
# MICROSSERVIÇOS — MedMatch
# Deployments + Services internos
# ─────────────────────────────────────────────────────────────────

# ── Auth Service ──────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: medmatch
  labels:
    app: medmatch
    component: auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: auth-service
          image: SEU_DOCKER_USER/medmatch-auth:latest
          ports:
            - containerPort: 8000
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: DB_HOST
              value: "credentials-db"
            - name: DB_NAME
              value: "medmatch_credentials"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: medmatch-credentials-db-secret
                  key: MYSQL_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: medmatch-credentials-db-secret
                  key: MYSQL_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: medmatch-jwt-secret
                  key: JWT_SECRET
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: medmatch
spec:
  selector:
    app: auth-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP

---
# ── Recovery Service ──────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recovery-service
  namespace: medmatch
  labels:
    app: medmatch
    component: recovery
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recovery-service
  template:
    metadata:
      labels:
        app: recovery-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: recovery-service
          image: SEU_DOCKER_USER/medmatch-recovery:latest
          ports:
            - containerPort: 8000
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: DB_HOST
              value: "credentials-db"
            - name: DB_NAME
              value: "medmatch_credentials"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: medmatch-credentials-db-secret
                  key: MYSQL_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: medmatch-credentials-db-secret
                  key: MYSQL_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: medmatch-jwt-secret
                  key: JWT_SECRET
---
apiVersion: v1
kind: Service
metadata:
  name: recovery-service
  namespace: medmatch
spec:
  selector:
    app: recovery-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP

---
# ── Doctors Service ───────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: doctors-service
  namespace: medmatch
  labels:
    app: medmatch
    component: doctors
spec:
  replicas: 1
  selector:
    matchLabels:
      app: doctors-service
  template:
    metadata:
      labels:
        app: doctors-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: doctors-service
          image: SEU_DOCKER_USER/medmatch-doctors:latest
          ports:
            - containerPort: 8000
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: DB_HOST
              value: "doctors-db"
            - name: DB_NAME
              value: "medmatch_doctors"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: medmatch-doctors-db-secret
                  key: MYSQL_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: medmatch-doctors-db-secret
                  key: MYSQL_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: medmatch-jwt-secret
                  key: JWT_SECRET
---
apiVersion: v1
kind: Service
metadata:
  name: doctors-service
  namespace: medmatch
spec:
  selector:
    app: doctors-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP

---
# ── Schedule Service ──────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: schedule-service
  namespace: medmatch
  labels:
    app: medmatch
    component: schedule
spec:
  replicas: 1
  selector:
    matchLabels:
      app: schedule-service
  template:
    metadata:
      labels:
        app: schedule-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: schedule-service
          image: SEU_DOCKER_USER/medmatch-schedule:latest
          ports:
            - containerPort: 8000
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: DB_HOST
              value: "scheduling-db"
            - name: DB_NAME
              value: "medmatch_scheduling"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: medmatch-scheduling-db-secret
                  key: MYSQL_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: medmatch-scheduling-db-secret
                  key: MYSQL_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: medmatch-jwt-secret
                  key: JWT_SECRET
---
apiVersion: v1
kind: Service
metadata:
  name: schedule-service
  namespace: medmatch
spec:
  selector:
    app: schedule-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP